import 'package:flutter/material.dart';


// New colors meticulously picked from your reference image.
class AppColours {
  /// The main, deep navy background color of the screen.
  static const Color background = Color(0xFF0B0F19);

  /// The color for cards, buttons, and the search bar.
  static const Color card = Color(0xFF1B2130);

  /// The color for primary text and selected icons.
  static const Color textPrimary = Color(0xFFFFFFFF);

  /// The color for secondary text (like timestamps) and unselected icons.
  static const Color textSecondary = Color(0xFF8A91A4);

  /// The color of the border line above the bottom navigation bar.
  static const Color border = Color(0xFF2C3246);
}
